About the Project
We’re building an MVP that enables users to create and share —all in the browser.

Goal

Build a small but real slice of our MVP to show you can handle browser video, backend, and product thinking.

What to build

In browser screen recording  
Record screen + mic using MediaRecorder API  
Start and stop controls ?? pending
Save output as .webm
Trim video  
Allow trimming start and end time  
Export trimmed video (webm or mp4)  
Use ffmpeg.wasm or server side ffmpeg (your call)
Upload + share  
Upload final video to storage (S3, R2, or mocked)
Generate a public share link  
Public page with embedded video player  
Basic analytics  
Track view count [todo]
Track watch completion percentage [todo]
Data should persist (db or file based)

Tech requirements Next.js + TypeScript  
Clean project structure Minimal but usable UI (Tailwind optional)

This is what they mentioned in the assignment
